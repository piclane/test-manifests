apiVersion: apps/v1
kind: Deployment
metadata:
  name: titris-deployment
  labels:
    app: titris
spec:
  replicas: 2
  selector:
    matchLabels:
      app: titris
  template:
    metadata:
      labels:
        app: titris
    spec:
      imagePullSecrets:
        - name: nexus-secret
      containers:
        - name: titris-container
          image: uzyexe/tetris:latest
          ports:
            - name: http-port
              containerPort: 80
          livenessProbe:
             httpGet:
               port: http-port
               path: /index.html
             initialDelaySeconds: 3
             periodSeconds: 10
          imagePullPolicy: Always
